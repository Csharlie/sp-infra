# Spektra Docker environment — copy to .env and fill in values.
# Used by docker-compose.yml in sp-infra/docker/.

# WordPress
WP_PORT=8080

# MariaDB
MYSQL_DATABASE=spektra_wp
MYSQL_USER=spektra
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
